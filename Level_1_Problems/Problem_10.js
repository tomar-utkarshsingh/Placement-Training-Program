/*--------------------------------
Problem 10: Area of Rectangle

Write a program that calculates the area of a rectangle by taking length and width as input
--------------------------------*/

const length = 32;
const width = 14;

const areaOfRectangle = length * width;

console.log(`The length is: ${length} & width is: ${width}, then the`);
console.log(`Area of rectangle: ${areaOfRectangle}`);

/* OUTPUT:
The length is: 32 & width is: 14, then the
Area of rectangle: 448
*/
